你现在是一个"互联网大厂级软件研发团队"的联合体，请以**资深产品经理 / 技术负责人 / Python桌面客户端架构师 / Linux应用工程师 / OpenFOAM集成工程师 / 测试工程师 / DevOps工程师 / 技术文档工程师**的身份，陪我从零开始，完整开发一个**基于 Python 的 OpenFOAM 流体力学仿真客户端软件**。

# 项目背景

* 我的目标：开发一个"基于 OpenFOAM 的流体力学仿真客户端软件"
* 软件形态：**桌面客户端软件，不是 Web 平台，不是前后端分离系统**
* 技术要求：**必须使用 Python 实现**
* 开发环境：**WSL**
* 运行环境：**WSL**
* OpenFOAM 环境：**OpenFOAM 已经安装在 WSL 中**
* 也就是说：**客户端软件与 OpenFOAM 都运行在同一个 WSL/Linux 环境中**
* 我的现状：我不懂完整的软件开发流程，希望你像互联网大厂一样，带我从零到完整做出这个项目
* 你的任务：必须按"互联网大厂标准研发流程"带我做，但同时优先考虑"一个人也能落地"的现实路径

# 重要约束

你必须基于以下现实前提来设计方案：

1. 这是一个**Python 桌面客户端软件**
2. **不是 Windows 调 WSL**
3. **不是浏览器 Web 系统**
4. **不是前后端分离平台**
5. 软件运行在 **WSL/Linux 图形环境** 中
6. OpenFOAM 也运行在同一个 WSL 环境中
7. 客户端可以直接以 Linux 本地进程方式调用 OpenFOAM 命令
8. 你必须考虑 WSL 下 GUI 运行、文件路径、Linux 权限、进程管理、打包与部署等问题

# 项目目标

这个客户端软件至少应支持以下能力：

1. 创建和管理仿真项目
2. 管理 OpenFOAM case
3. 编辑常用仿真参数
4. 直接调用本机 WSL 环境中的 OpenFOAM 命令
5. 执行网格生成、求解、后处理
6. 查看运行日志和任务状态
7. 管理仿真输出文件
8. 提取关键结果并可视化展示
9. 为未来扩展更多求解器、模板、物理模型预留结构

# 你必须遵守的工作方式

你必须严格按照"大厂研发流程"推进，每一步都要告诉我：

1. 这一阶段的**目标**
2. 这一阶段通常由哪些**角色**负责
3. 每个角色分别做什么
4. 这一阶段需要的**知识**
5. 这一阶段涉及的**技术、框架、工具**
6. 这一阶段的**输入物**
7. 这一阶段的**输出物**
8. 这一阶段的**详细执行步骤**
9. 这一阶段常见的**坑、风险、边界条件**
10. 这一阶段完成的**验收标准**
11. 这一阶段结束后如何进入下一阶段

# 你必须覆盖的软件研发全过程

请把整个项目拆成完整的软件研发生命周期，至少覆盖以下部分，并且每一部分都要讲到可执行层面：

## 第1部分：项目定义与需求分析

请输出：

* 项目愿景
* 产品目标
* 用户画像
* 核心使用场景
* MVP 范围
* 非 MVP 范围
* 功能需求清单
* 非功能需求清单（性能、稳定性、可维护性、可扩展性、易用性、安全性）
* 一份像大厂那样的 PRD 结构

并说明：

* 这一阶段有哪些角色
* 每个角色怎么协作
* 如何定义一个"可落地的桌面仿真客户端"
* 如何避免一开始做成超大而不可控的系统

## 第2部分：桌面客户端产品形态设计

请输出：

* 这是一个什么类型的客户端软件
* 主界面结构
* 页面 / 窗口 / 面板划分
* 菜单栏、工具栏、项目树、参数配置区、日志区、结果区等设计建议
* 单文档、多文档、工作区式界面哪种更合适
* 本地软件的数据流如何组织
* 用户操作路径设计
* 如何让"不会 CFD 的用户"也能使用

并说明：

* 这一阶段由哪些角色负责
* 为什么这么设计
* 如何在专业性与易用性之间平衡

## 第3部分：系统架构设计

请输出客户端软件的整体架构，要求明确分层，至少包括：

* UI 展示层
* 应用控制层
* 业务逻辑层
* OpenFOAM 集成层
* Linux/WSL 本地进程调用层
* 文件管理层
* 数据存储层
* 日志与监控层

请详细说明：

* 每一层的职责
* 每一层之间如何调用
* 哪些模块必须解耦
* 如何避免 UI 代码和 OpenFOAM 调用逻辑耦合
* 如何避免业务逻辑散落在界面事件中
* 如何为未来扩展插件、求解器、模板留接口

## 第4部分：Python 技术栈选型

请输出并比较以下内容：

* GUI 框架推荐：PySide6 / PyQt / Tkinter / wxPython 等
* 数据可视化方案：matplotlib / pyqtgraph / vtk / pyvista
* 本地数据库方案：SQLite / TinyDB / JSON 文件
* 配置管理方案
* 日志方案
* 任务执行方案：threading / multiprocessing / asyncio / QThread
* 打包发布方案：PyInstaller / Nuitka / Linux打包方式
* 测试方案：pytest / unittest / pytest-qt
* 代码质量方案：black / ruff / mypy

并明确给出：

* 推荐最终技术栈
* 为什么选它
* 不选其他方案的原因
* 对一个人开发最稳妥的路线是什么

## 第5部分：大厂式研发流程与角色分工

请输出：

* 产品经理
* 技术负责人
* Python客户端工程师
* OpenFOAM集成工程师
* 测试工程师
* 发布工程师
* UI/UX设计师
* 项目经理

分别负责什么。

并说明：

* 需求评审怎么做
* 技术方案评审怎么做
* 架构评审怎么做
* 任务拆解怎么做
* 代码评审怎么做
* Bug 流程怎么做
* 发布流程怎么做
* 版本管理怎么做
* 风险管理怎么做

特别说明：

* 如果我是一个人开发，如何一个人扮演这些角色
* 哪些流程必须保留
* 哪些流程可以简化

## 第6部分：模块拆分设计

请把客户端拆成可实现模块，并逐个说明：

* 模块名
* 模块职责
* 主要类设计
* 输入输出
* 数据流
* 状态流转
* 异常处理
* 日志设计
* 测试点

至少覆盖以下模块：

1. 项目管理模块
2. Case 模板管理模块
3. 几何/网格管理模块
4. 参数配置模块
5. OpenFOAM 配置文件生成模块
6. Linux 本地命令调用模块
7. 仿真任务执行模块
8. 任务状态与日志跟踪模块
9. 结果文件管理模块
10. 后处理与可视化模块
11. 本地配置与设置模块
12. 错误处理与诊断模块

## 第7部分：本地数据设计

请输出：

* 本地项目目录结构设计
* 工作空间目录结构设计
* case 目录如何组织
* 模板目录如何组织
* 日志目录如何组织
* 输出结果目录如何组织
* 本地数据库如何设计
* 哪些数据存 SQLite
* 哪些数据存 JSON / YAML
* 哪些数据直接存文件系统
* 如何记录任务状态、运行历史、文件索引、结果元数据

## 第8部分：桌面客户端 UI 设计

请输出：

* 页面与窗口结构
* 主窗口布局
* 项目列表/项目树设计
* 参数配置页设计
* 求解运行页设计
* 日志面板设计
* 结果展示页设计
* 设置页设计
* 错误提示与交互设计
* 新手模式 / 专家模式设计建议

并说明：

* 每个界面要展示什么
* 每个按钮对应什么动作
* 界面状态如何和业务状态同步
* 如何防止用户误操作

## 第9部分：Python 工程结构设计

请输出：

* 推荐项目目录结构
* 包结构设计
* 模块命名建议
* 分层代码组织方式
* DTO / 实体 / 服务 / 控制器 / 适配器如何组织
* 配置文件怎么放
* 资源文件怎么放
* 图标、模板、脚本怎么管理
* 如何写出可维护的 Python 桌面应用

## 第10部分：OpenFOAM 集成专项设计

这是重点，请详细展开：

请输出：

* Python 客户端如何直接调用同一 WSL/Linux 环境中的 OpenFOAM
* 如何执行 blockMesh、snappyHexMesh、simpleFoam、pisoFoam、paraFoam 等命令
* 如何获取命令实时输出
* 如何捕获 stderr / stdout
* 如何判断任务执行状态
* 如何处理中断、失败、重试
* 如何管理 case 工作目录
* 如何避免多个任务互相污染
* 如何读写 OpenFOAM 配置文件
* 如何把 controlDict、fvSchemes、fvSolution、transportProperties、thermophysicalProperties 等配置抽象成客户端表单
* 如何设计 case 模板机制
* 如何支持多求解器扩展
* 如何提取 residuals、forces、probes 等结果
* 如何把结果转换为客户端可展示的数据结构
* 如何处理 OpenFOAM 环境变量与 shell 初始化
* 如何处理 source OpenFOAM 环境脚本之后再执行命令
* 如何在 GUI 应用中稳定调用 bash -lc 或等价方式执行 OpenFOAM 命令

并且明确说明：

* 哪些能力先做 MVP
* 哪些能力后续再做
* 哪些部分最容易踩坑

## 第11部分：任务执行与并发设计

请输出：

* 桌面客户端中后台任务怎么设计
* UI 线程和任务线程怎么分离
* 如何防止界面卡死
* 如何做任务队列
* 如何支持单任务 / 多任务
* 如何显示进度、状态、日志
* 如何中止任务
* 如何做失败恢复
* 如何处理资源占用

## 第12部分：测试体系设计

请输出：

* 单元测试
* 集成测试
* GUI 测试
* OpenFOAM 调用测试
* 回归测试
* 性能测试
* 稳定性测试
* 打包后验证测试
* 用户验收测试

并说明：

* 每类测试测什么
* 如何准备测试环境
* 如何准备测试 case
* 发布前检查清单是什么

## 第13部分：打包、发布与部署

请输出：

* WSL/Linux 环境下客户端如何打包
* 如何附带资源文件
* 如何处理 Python 依赖
* 如何做版本管理
* 如何做安装与运行说明
* 如何适配不同 Linux/WSL 发行版
* 如何检查 OpenFOAM 运行条件
* 如何设计启动检查与环境诊断功能

## 第14部分：安全与健壮性设计

请输出：

* 命令执行安全
* 用户输入校验
* 防止路径错误
* 防止命令注入
* 文件读写安全
* 崩溃恢复
* 日志审计
* 错误诊断
* 对 OpenFOAM 环境问题的友好提示

## 第15部分：项目实施路线图

请输出：

* 按阶段或按 Sprint 的实施计划
* MVP 第一版先做什么
* 第二版做什么
* 每阶段验收标准
* 风险点和缓解策略
* 一人开发版路线图
* 小团队版路线图

## 第16部分：真正开始开发

你不能只讲方案，必须进入"可落地开发模式"。

你必须输出：

1. 推荐的最终技术栈
2. 推荐的项目目录结构
3. 第一版 MVP 功能清单
4. 第一个 Sprint 的任务拆解
5. 先写哪些文档
6. 先创建哪些 Python 文件
7. 每个文件的职责
8. 第一批代码应该先实现什么
9. 你接下来如何一步一步带我开发

# 输出格式要求

你必须严格按以下结构输出：

## A. 项目总览

## B. 全流程研发阶段拆解

### 阶段1：...

* 目标
* 角色
* 知识
* 技术
* 输入
* 输出
* 执行步骤
* 风险
* 完成标准

### 阶段2：...

（依次展开）

## C. 推荐技术栈与选型理由

## D. 客户端系统架构设计

## E. 模块级详细设计

## F. 本地数据与目录设计

## G. UI 设计

## H. Python 工程结构设计

## I. OpenFOAM 集成设计

## J. 测试与发布设计

## K. 项目实施路线图

## L. 第一版 MVP 方案

## M. 第一个 Sprint 任务拆解

## N. 现在就开始：先输出"PRD v1 + 技术方案 v1 + 模块拆分 v1 + 第一阶段实施建议"

# 额外要求

* 不要只给框架，必须给细节
* 必须优先给我"一人可落地"的现实方案，再补充"大厂完整版"
* 必须严格围绕"Python 桌面客户端软件"展开，不要跑到 Web 平台方案
* 必须严格结合"WSL 内开发、WSL 内运行、OpenFOAM 已安装"的现实环境
* 必须把"UI层 / 应用控制层 / 业务层 / OpenFOAM集成层 / Linux本地进程调用层 / 本地存储层"区分清楚
* OpenFOAM 部分必须结合真实工程实践
* 要主动控制复杂度，先做 MVP，再做增强版
* 必须考虑 WSL GUI 的现实问题，例如图形显示环境、依赖、运行方式，但不要把架构复杂化
* 输出时尽量使用分层结构、清单、表格化表达、文本流程图，方便我直接执行

从现在开始，先输出：

1. PRD v1
2. 技术方案 v1
3. 模块拆分 v1
4. 第一阶段实施建议